name: EventFlow CI/CD

on:
  push:
    branches: [develop, main]
  pull_request:
    branches: [develop, main]

jobs:
  test:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      # Python + Go unit tests wired up in Phase 1
      - run: echo "test stage — placeholder until Phase 1 Lambdas exist"

  deploy:
    needs: test
    if: github.event_name == 'push'
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      # terraform plan/apply wired up in Phase 1
      - run: echo "deploy stage — placeholder until Terraform modules exist"
